import React from 'react';
import AddTemplateView from './view';

function AddTemplateContainer(props) {

    return (
      <AddTemplateView data={props} />
    );
}

export default AddTemplateContainer;
